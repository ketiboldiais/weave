import { describe, expect, it } from "vitest";
// import { integer, natural, posint, ufloat } from "../src/index.js";
/*
describe("num", () => {
  it("should parse a positive integer", () => {
    const a = `12`;
    const r = posint.parse(a).result.value;
    const e = { n: 12, type: "Z" };
    expect(r).toEqual(e);
  });
  it("should parse a positive integer", () => {
    const a = `+821`;
    const r = posint.parse(a).result.value;
    const e = { n: 821, type: "Z" };
    expect(r).toEqual(e);
  });
  it("should parse a natural number", () => {
    const a = `0`;
    const b = natural.parse(a).result.value;
    const c = { n: 0, type: "Z" };
    expect(b).toEqual(c);
  });
  it("should parse a natural number", () => {
    const a = `32`;
    const b = natural.parse(a).result.value;
    const c = { n: 32, type: "Z" };
    expect(b).toEqual(c);
  });
  it("should parse an integer", () => {
    const a = `+19`;
    const b = integer.parse(a).result.value;
    const c = { n: 19, type: "Z" };
    expect(b).toEqual(c);
  });
  it("should parse an integer", () => {
    const a = `-128`;
    const b = integer.parse(a).result.value;
    const c = { n: -128, type: "Z" };
    expect(b).toEqual(c);
  });
  it("should parse an unsigned float", () => {
    const a = `0.0`;
    const b = ufloat.parse(a).result.value;
    const c = { n: 0, type: "R" };
    expect(b).toEqual(c);
  });
  it("should parse an unsigned float", () => {
    const a = `2.0`;
    const b = ufloat.parse(a).result.value;
    const c = { n: 2, type: "R" };
    expect(b).toEqual(c);
  });
  it("should parse an unsigned float", () => {
    const a = `2.520910`;
    const b = ufloat.parse(a).result.value;
    const c = { n: 2.520910, type: "R" };
    expect(b).toEqual(c);
  });
});
*/